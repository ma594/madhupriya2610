import java.util.scanner;
public class SecondLargest
{
    public static void main(String args[])
    {
       int temp;
       for(int i=0;i<total;i++)
        {
            for(int i=0;i<total;i++)
            {
                for(int j=i+1;j<total;j++)
                {
                    if(a[i]>a[j])
                    {
                        temp=a[i];
                        a[i]=a[j];
                        a[j]=temp;
                    }
                }
                    return a[total_2];
            }
            System.out.println("second largest:"+getsecondlargest(i));
        }      
             }                    
                }